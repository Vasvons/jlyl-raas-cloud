fix(content): 修复 POST /images 500 错误 - tags 字段类型不匹配

image_library.tags 是 TEXT[] 类型，pg 驱动需要 JS 数组，
但代码用 JSON.stringify 传了字符串，导致 PostgreSQL 类型转换失败。
- createImage: JSON.stringify(data.tags || []) → data.tags || []
- updateImage: JSON.stringify(data[key]) → data[key]
- 路由 catch 块增加 console.error 记录错误堆栈
