fix: 修复Dockerfile.web使用standalone模式但项目是export模式的问题

项目使用 Next.js output: 'export' 静态导出，产物在 out/ 目录，
但 Dockerfile.web 期望 output: 'standalone'（产物在 .next/standalone）。

修改为使用 nginx:alpine 托管 out/ 目录的静态文件：
- 新增 docker/nginx-web.conf 容器内 nginx 配置
- 监听 3011 端口
- /api/ 代理到 server:3002（docker-compose 网络）
- SPA 路由回退支持 Next.js 静态导出
