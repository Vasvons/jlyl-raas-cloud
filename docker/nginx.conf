# jlyl-cloud Nginx 配置
# 将此文件放到服务器 /etc/nginx/conf.d/jlyl-cloud.conf
# 然后执行: nginx -t && nginx -s reload

# HTTP 跳转 HTTPS
server {
    listen 80;
    server_name report.jlyl.net.cn;

    # 如果有 SSL 证书，跳转到 HTTPS
    # return 301 https://$host$request_uri;

    # 如果没有 SSL 证书，直接代理
    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # API 请求代理到后端
    location /api/ {
        proxy_pass http://127.0.0.1:3002/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 静态资源缓存
    location /_next/static/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_cache_valid 200 1y;
        add_header Cache-Control "public, immutable";
    }

    client_max_body_size 10m;
}

# HTTPS 配置（如果有 SSL 证书）
# server {
#     listen 443 ssl http2;
#     server_name report.jlyl.net.cn;
#
#     ssl_certificate /etc/nginx/ssl/jlyl.net.cn.pem;
#     ssl_certificate_key /etc/nginx/ssl/jlyl.net.cn.key;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     location / {
#         proxy_pass http://127.0.0.1:3001;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
#
#     location /api/ {
#         proxy_pass http://127.0.0.1:3002/;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location /_next/static/ {
#         proxy_pass http://127.0.0.1:3001;
#         proxy_cache_valid 200 1y;
#         add_header Cache-Control "public, immutable";
#     }
#
#     client_max_body_size 10m;
# }
